@page
@model CArsssss.Pages.Man.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Manufacter[0].Ford)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Manufacter[0].AStonMartin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Manufacter[0].Cheverolet)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Manufacter) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ford)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AStonMartin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cheverolet)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
